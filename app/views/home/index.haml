- @cont.each do |co|
  = content_tag(:p, "#{co.first_name} #{co.last_name} - #{co.calculated_end_time}")
= content_tag(:p, 'Nowy zawodnik')
= form_tag("", class: 'form', style: 'color: red')
= label_tag "Imię", :first_name
= input_tag(:contestant, :first_name)
= submit_tag "Dodaj"